# ============ Phase 1: Web 框架 / DB / 认证 / 客户端 ============
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.27.2
redis==5.2.1
asyncmy==0.2.9
SQLAlchemy[asyncio]==2.0.36
# pydantic>=2.11.5 是 llama-index-workflows（llama-index-core 0.14.24 依赖）的硬性要求，
# 2.10.4 在干净环境 Docker 构建必然 ResolutionImpossible；2.13.4 为宿主机已验证版本。
pydantic==2.13.4
pydantic-settings==2.7.0
bcrypt==4.2.1
PyJWT==2.10.1
python-multipart==0.0.20

# ============ Phase 2: RAG ============
sentence-transformers==3.3.1
# torch 由 Dockerfile.backend 单独安装 CPU 版（PyPI 默认 CUDA 版过大）

# ============ P1: LlamaIndex 检索层 + Milvus ============
# P1 将 RAG 检索层迁到 LlamaIndex（Milvus 存储 + 交叉编码重排）。
# 版本按 pip 解析结果校准：milvus integration 1.1.0 要求 pymilvus>=2.6.7,<3。
llama-index-core==0.14.24
llama-index-vector-stores-milvus==1.1.0
llama-index-embeddings-huggingface==0.7.0
# pymilvus 与服务端 milvus v2.5.4 配套（见 docker-compose.yml）
pymilvus==2.6.17

# ============ Phase 3: 状态机 ============
langgraph==0.2.60
langchain-core==0.3.25

# ============ Phase 5: 测试 ============
pytest==8.3.3
pytest-asyncio==0.24.0
